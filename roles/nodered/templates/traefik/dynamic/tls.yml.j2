tls:
  certificates:
    - certFile: /certs/{{ nodered_service_domain }}.crt
      keyFile: /certs/{{ nodered_service_domain }}.pem
  options:
    default:
      minVersion: VersionTLS12
      sniStrict: true
